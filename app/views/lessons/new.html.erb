<div class="yelbox">
    <div class="form-container-yel">
        <h1>Créer une lesson</h1>

        <%= simple_form_for(@lesson) do |f| %>
        <div class="form-inputs">
            <%= f.input :course,
                        required: true %>
            <%= f.input :date,
                        required: true %>
            <%= f.input :length,
                        required: true %>
            <%= f.input :beginning,
                        required: true %>
        </div>

        <div class="form-actions">
            <%= f.button :submit, "Create Lesson" %>
        </div>
        <% end %>
    </div>
</div>