<div class="formbox yel-bg">
    <div class="form-container-yel">
        <h1>Prendre rendez-vous</h1>

         <div class="lesson-card">                            
            <p>Date : <%= @lesson.date %></p>
            <p>Heure : <%= @lesson.beginning %></p>
            <p>Durée : <%= @lesson.length %></p>
        </div>

        <%= simple_form_for [ @lesson, @rdv ] do |f| %>
            <div class="form-inputs">
                <%= f.input :course,
                            required: true %>
            </div>

            <div class="form-actions">
                <%= f.button :submit, "Prendre Rendez-vous", class: "btn-base btn-blue" %>
            </div>
        <% end %>

        <%= link_to "Retour", lessons_path, class: "nav-link" %>
    </div>
</div>